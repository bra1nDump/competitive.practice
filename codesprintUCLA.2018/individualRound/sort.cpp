#include <iostream>
#include <vector>

using namespace std;

int main() {
  int size;

  cin >> size;

  vector<int> arrayToSort;
  for(int i = 0; i < size; i += 1) {
    int newElement;
    cin >> newElement;

    arrayToSort.push_back(newElement);
  }

  for(int i = 0; i < size; i += 1) {
    
  }
}
